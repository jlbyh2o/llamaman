// Package setup is the first-run wizard: the step state machine that decides
// which step is active, complete or skippable, and the service methods each step
// calls (DESIGN sections 1 and 11).
package setup
