// Package netutil handles port selection: walking a range, probing whether a
// port is actually free rather than merely unrecorded, and allocating from the
// reserved range instances draw from (DESIGN section 1).
package netutil
