// Package hf is the Hugging Face HTTP client: model search, model metadata, the
// repository file tree, the README behind a model card, and resolve URLs for
// downloads. It handles authentication with the stored token and the gated-repo
// responses that come back without one (DESIGN sections 1 and 7).
package hf
