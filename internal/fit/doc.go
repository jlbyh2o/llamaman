// Package fit is the fit calculator: a pure function from a model shape, a
// FlagSet, the visible devices and a calibration table to a report saying
// whether the configuration will run and where its memory goes. No I/O, no
// clock, no database, and nothing outside the standard library (DESIGN sections
// 1 and 8).
package fit
